const MockConfig = {
  /** The root path for all API calls (e.g. '/api/') */
  apiPath: '/api/',
  /** A list of endpoints to mock */
  endpoints: [
    { path: 'loadInfo' }
  ]
};
export default MockConfig;
